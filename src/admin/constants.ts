/**
 * Caminho da rota de administração (sem link no site público).
 * Altere aqui se quiser outra URL, por exemplo `/config-interna`.
 */
export const ADMIN_SITE_CONFIG_PATH = "/adm";

/** Path relativo à base da API para leitura/escrita da configuração (documentado em BACKEND_INTEGRATION.md). */
export const SITE_CONFIG_API_PATH = "/api/site-config";
