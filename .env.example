# Base URL do backend (sem barra no fim).
# O front faz pedidos a: ${VITE_ADMIN_API_BASE_URL}/api/...
#
# IMPORTANTE: use o mesmo URL que testa no Postman.
# Backend local (npm run dev em /backend) → use localhost.
# Backend no Render → use a URL do Render.
#
# Desenvolvimento local:
# VITE_ADMIN_API_BASE_URL=http://localhost:3000
#
# Produção (Vercel → Render):
# VITE_ADMIN_API_BASE_URL=https://projeto-arquice.onrender.com
#
# Se deixar vazio ou comentado, a página /adm carrega os valores de siteConfig.ts
# e o botão "Guardar" pede esta variável (não grava em lado nenhum).
#
# Deploy na Vercel: defina VITE_ADMIN_API_BASE_URL nas Environment Variables
# do projeto e faça redeploy (o .env local não é enviado para produção).
#
# Após alterar o .env local, reinicie o servidor Vite (npm run dev).
# VITE_ADMIN_API_BASE_URL=
